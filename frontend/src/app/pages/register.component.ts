import { Component } from '@angular/core'; import { Router } from '@angular/router'; import { AuthService } from '../services/auth.service';
@Component({template:`<div class="row justify-content-center"><div class="col-md-5"><div class="card p-4"><h3>Register</h3><input class="form-control my-2" placeholder="Username" [(ngModel)]="username"><input class="form-control my-2" placeholder="Email" [(ngModel)]="email"><input class="form-control my-2" type="password" placeholder="Password" [(ngModel)]="password"><button class="btn btn-success btn-block" (click)="register()">Create Account</button><p class="text-danger mt-2">{{err}}</p></div></div></div>`}) export class RegisterComponent{username='';email='';password='';err='';constructor(private auth:AuthService,private router:Router){} register(){this.auth.register({username:this.username,email:this.email,password:this.password}).subscribe(r=>{this.auth.save(r);this.router.navigate(['/'])},e=>this.err='Registration failed')}}
